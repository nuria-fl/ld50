import { BaseScene } from "../utils/BaseScene";

export class Town extends BaseScene {
  constructor() {
    super("town");
  }

  preload() {}

  create() {
    super.create();
  }

  update() {
    super.update();
  }
}
